/**
 * Test case for banner.
 * Runs with mocha.
 */
'use strict'

const banner = require('../lib/banner.js')
const assert = require('assert')
const co = require('co')

describe('banner', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Banner', () => co(function * () {

  }))
})

/* global describe, before, after, it */
